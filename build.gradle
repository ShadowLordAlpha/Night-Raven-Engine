apply plugin: 'java'
apply plugin: 'eclipse'
apply plugin: 'application'

repositories {
    mavenCentral()
    maven { url "https://oss.sonatype.org/content/repositories/snapshots/" }
}

mainClassName = "io.github.cybernetic_shadow.echo.core.Main"
applicationDefaultJvmArgs = [""]
javadoc.options.tags = ['sla.remarks:a:Remarks:', 'sla.ts:a:Thread Safety:', 'sla.warn:a:Warning:', 'sla.reen:a:Reentrancy:']

sourceCompatibility = 1.8
targetCompatibility = 1.8
project.ext.lwjglVersion = "3.0.0-SNAPSHOT" // This is the snapshot version of LWJGL and its probably broken in some way :p
project.ext.jomlVersion = "+"
project.ext.slf4japiVersion = "+"
project.ext.slf4jlogVersion = "+"

dependencies {
    compile "org.lwjgl:lwjgl:${lwjglVersion}"
    compile "org.lwjgl:lwjgl-platform:${lwjglVersion}:natives-windows"
    compile "org.lwjgl:lwjgl-platform:${lwjglVersion}:natives-linux"
    compile "org.lwjgl:lwjgl-platform:${lwjglVersion}:natives-osx"
    compile "org.joml:joml:${jomlVersion}"
    compile "org.slf4j:slf4j-api:${slf4japiVersion}"
    compile "org.slf4j:slf4j-simple:${slf4jlogVersion}"
	compile project(':Math'), project(':Audio')
}

jar {
	baseName = 'Night-Raven'
	version = '0.1.0-a'
}

task wrapper(type: Wrapper) {
	gradleVersion = '2.12'
}

subprojects {

	apply plugin: 'java'
	apply plugin: 'eclipse'
	group = 'sla.nightraven'
	version '0.0.1-a'
	
	repositories {
		mavenCentral()
		
	}
	
	dependencies {
	    compile "org.slf4j:slf4j-api:${slf4japiVersion}"
		testCompile "org.slf4j:slf4j-simple:${slf4japiVersion}"
		testCompile "junit:junit:4.12"
	}
}

project(':Math') {

}

project(':Audio') {

	dependencies {
		compile project(':Math')
	}
}
